# ZZZGlip 録画・WIPEOUT検知 仕様書

> **【開発鉄則】**
> **原因不明のまま「再起動」や「リトライ」による解決を図ることは厳禁。必ず真因を特定し、根本的な対策を講じること。**

---

## 1. システム構成

### 1.1 属性タグ (AttributionTag) の配置
- **マニフェスト**: `<attribution>` タグは必ず `<manifest>` 直下に配置すること。
- **Context**: システムサービスの取得には `createAttributionContext("glip_recorder")` を通した Context を使用すること。

### 1.2 VirtualDisplay の永続性
- **一回限りの生成**: 録画セッション中、`VirtualDisplay` を再作成してはならない。画面回転時は `resize()` と `setSurface()` で対応すること。再作成は Android 14 で `SecurityException` を引き起こす。

---

## 2. WIPEOUT検知ロジック

### 2.1 検知ループの堅牢性（防衛策）
- **リソース防護**: `PixelCopy.request` 実行前に `surface.isValid` を必須チェック。これはリトライではなく、無効なリソースへのアクセスによる**アプリ全体の強制終了を防ぐための「防衛」**である。
- **ループ間隔**: 300ms間隔（約 3.3 FPS）で実行。複数回の累積判定は行わず、1回の高精度判定で即時保存を開始する（決定的な瞬間を逃さないため）。

### 2.2 ハイブリッド判定アルゴリズム
OCR負荷の低減と誤検知防止のため、以下の条件を組み合わせる。

1. **黄色判定 (OCRゲート)**: 画面中央付近に黄色ピクセルが **0.015** 以上の密度で存在すること。これをOCR処理を開始するかどうかの判断基準とする。
2. **幾何学的フィルタリング (最優先)**:
   - **配置**: テキストブロックの中心が画面の中央付近（Y軸 0.3 〜 0.7）にあること。
   - **サイズ**: テキストの高さが画面全体の **8% 以上**（0.08以上）であること。
3. **断片結合 (Fragment Aggregation)**: OCRが文字を分割して読み取った場合、水平方向に近接（高さの1.5倍以内）していれば1つの単語として統合して判定する。
4. **形状・内容判定**: 
   - テキスト全体の**アスペクト比が 2.5 〜 8.5** であること。
   - `levenshteinDistance` による「WIPEOUT」とのファジー一致、または特徴文字（W, I, P...）の含有率で確定する。

### 2.3 録画・保存シーケンス
- **保存シーケンス (Timing)**: 
  - WIPEOUT検知から**5秒間**待機した後に保存を開始する。
  - 保存する動画の長さは**10秒間** (`10000L`) とする。
- **キーフレーム同期（Keyframe Alignment）**: セグメント（30秒ファイル）の切り替わりは、必ずビデオエンコーダからのキーフレーム（`BUFFER_FLAG_KEY_FRAME`）を待機して実行すること。これを怠ると、マージ後の動画が切り替わり目でフリーズする。
- **音声同期（A/V Sync）**: 音声の Presentation Time Stamp (PTS) は、読み込んだサンプル数 (`totalAudioSamples * 1M / 48000`) から算出し、ビデオとのズレ（Drift）を物理的に排除する。
- **エラー処理**: 保存処理中にエラーが発生した場合は、不完全なファイルを破棄し、中途半端なデータを残さないこと。

---

## 3. UI/UX・広告連携

### 3.1 視覚的フィードバック
- **「WO!」ボタン**: 保存処理中にボタンテキストを「WO!」に変更すること。
- **透過制御**: 保存中はボタンの透過度を下げ、進捗インジケータを表示する。

### 3.2 広告表示 (Monetization)
- **表示タイミング**: 保存完了時の割り込み広告（インタースティシャル）は、ユーザー体験を損なうため廃止する。
- **表示場所**: アプリ起動後のメイン画面（録画ボタンの周辺）にバナー広告を表示することで、アプリ利用を妨げずに収益化を図る。
