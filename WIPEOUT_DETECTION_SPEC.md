# ZZZGlip 録画・WIPEOUT検知 仕様書

> **【開発鉄則】**
> **原因不明のまま「再起動」や「リトライ」による解決を図ることは厳禁。必ず真因を特定し、根本的な対策を講じること。**

---

## 1. システム構成

### 1.1 属性タグ (AttributionTag) の配置
- **マニフェスト**: `<attribution>` タグは必ず `<manifest>` 直下に配置すること。
- **Context**: システムサービスの取得には `createAttributionContext("glip_recorder")` を通した Context を使用すること。

### 1.2 VirtualDisplay の永続性
- **一回限りの生成**: 録画セッション中、`VirtualDisplay` を再作成してはならない。画面回転時は `resize()` と `setSurface()` で対応すること。再作成は Android 14 で `SecurityException` を引き起こす。

---

## 2. WIPEOUT検知ロジック

### 2.1 検知ループの堅牢性（防衛策）
- **リソース防護**: `PixelCopy.request` 実行前に `surface.isValid` を必須チェック。これはリトライではなく、無効なリソースへのアクセスによる**アプリ全体の強制終了を防ぐための「防衛」**である。
- **ループ間隔**: 300ms間隔（約 3.3 FPS）で実行。複数回の累積判定は行わず、1回の高精度判定で即時保存を開始する（決定的な瞬間を逃さないため）。

### 2.2 ハイブリッド判定アルゴリズム
OCR負荷の低減と誤検知防止のため、以下の条件を組み合わせる。

1. **黄色判定 (OCRゲート)**: 画面中央付近に黄色ピクセルが **0.015** 以上の密度で存在すること。これをOCR処理を開始するかどうかの判断基準とする。
2. **幾何学的フィルタリング (最優先)**:
   - **配置**: テキストブロックの中心が画面の中央付近（Y軸 0.3 〜 0.7）にあること。
   - **サイズ**: テキストの高さが画面全体の **8% 以上**（0.08以上）であること。
3. **断片結合 (Fragment Aggregation)**: OCRが文字を分割して読み取った場合、水平方向に近接（高さの1.5倍以内）していれば1つの単語として統合して判定する。
4. **形状・内容判定**: 
   - テキスト全体の**アスペクト比が 2.5 〜 8.5** であること。
   - `levenshteinDistance` による「WIPEOUT」とのファジー一致、または特徴文字（W, I, P...）の含有率で確定する。

### 2.3 録画・保存シーケンス
- **保存シーケンス (Timing)**: 
  - WIPEOUT検知から**5秒間**待機した後に保存を開始する。
  - 保存する動画の長さは**10秒間** (`10000L`) とする。
- **向きの変化への耐性 (Orientation-Aware)**:
  - セグメントファイル名に `_L` (Landscape) / `_P` (Portrait) を付与し、マージ時に「数が多い方の向き」を優先して結合する。これにより、録画中の画面回転による動画破損を防ぐ。
- **キーフレーム同期（Keyframe Alignment）**: セグメント（30秒ファイル）の切り替わりは、必ずビデオエンコーダからのキーフレーム（`BUFFER_FLAG_KEY_FRAME`）を待機して実行すること。これを怠ると、マージ後の動画が切り替わり目でフリーズする。
- **音声同期（A/V Sync & Capture）**: 
  - `AudioPlaybackCapture` を使用し、システムの MEDIA/GAME 出力を直接取得する。
  - 音声の Presentation Time Stamp (PTS) は、読み込んだサンプル数 (`totalAudioSamples * 1M / 48000`) から算出し、ビデオとのズレ（Drift）を物理的に排除する。
- **ストレージ・エラー処理**: 
  - バッファセグメントは最大20個（約10分）に制限し、古いものから自動削除してストレージを保護する。
  - 保存処理中にエラーが発生した場合は、不完全なファイルを破棄し、中途半端なデータを残さないこと。

---

## 3. UI/UX・広告連携

### 3.1 視覚的フィードバック
- **「WO!」ボタン**: 保存処理中にボタンテキストを「WO!」に変更すること。
- **透過制御**: 保存中はボタンの透過度を下げ、進捗インジケータを表示する。

### 3.2 広告表示 (Monetization)
- **表示タイミング**: 保存完了時の割り込み広告（インタースティシャル）は、ユーザー体験を損なうため廃止する。
- **表示場所**: アプリ起動後のメイン画面（録画ボタンの周辺）にバナー広告を表示することで、アプリ利用を妨げずに収益化を図る。

---

## 4. Android互換性・安定性確保

### 4.1 起動・リソース管理の「待機」戦略
- **起動ディレイ (Startup Delay)**: `START_RECORDING` 受信後、MediaProjection等の初期化を開始する前に必ず **1000ms のディレイ**を置くこと。これは、前回のセッション資源がOS側で完全に解放されるのを待ち、Android 14以降での `SecurityException` を防ぐための必須の防衛策である。
- **セッションの隔離**: 録画開始時に `cleanLegacyFiles()` を実行し、キャッシュディレクトリ内の古いセグメントファイルを完全に物理削除すること。これを怠ると、マージ時に過去のセッションの動画が混入する原因となる。

### 4.2 音声キャプチャの広域互換性
- **USAGEの網羅**: `AudioPlaybackCaptureConfiguration` のビルド時、`USAGE_MEDIA` だけでなく `USAGE_GAME`, `USAGE_UNKNOWN`, `USAGE_ASSISTANT` 等をすべて含めること。これは、Samsungデバイス等の一部機種でゲーム音が特定のUSAGEに割り振られ、音が拾えない問題を回避するための実地対策である。

### 4.3 リソースの永続管理
- **AudioRecord の単一生存権**: 録画セッション中、`AudioRecord` インスタンスは一度だけ生成し、セッション終了まで保持すること。ループ内での頻繁な破壊・再生成は Android 14 のフォアグラウンドサービス制限により、音声取得の失敗やクラッシュを引き起こす。

### 4.4 実行権限とオーディオ優先順位の確保
- **Foreground Service 属性の厳格化**: Android 14以降、録画と音声キャプチャを並行させるため、サービス開始時に `FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION` と `FOREGROUND_SERVICE_TYPE_MICROPHONE` の両方を明示的に申告すること。
- **オーディオフォーカス (Audio Focus) の戦略的取得**: `AudioRecord` の開始直前に `AUDIOFOCUS_GAIN` を要求し、OSに対して音声キャプチャの優先順位を確保する。これにより、システム音声取得の安定性を高める。

---

## 5. パフォーマンス・UX維持の仕組み

### 5.1 高速マージ（再エンコード回避）
- **サンプルコピー方式**: `fastMergeFiles` において、動画の結合時に再エンコード（Decode -> Encode）を一切行わず、`MediaExtractor` で抽出したデータを直接 `MediaMuxer` に書き込む。これにより、長時間のバッファでも数秒で書き出しを完了させ、端末の発熱とバッテリー消費を最小限に抑える。

### 5.2 システム連携とクリーンアップ
- **強制終了の検知**: `MediaProjection.Callback` の `onStop()` を確実に監視し、システムやユーザーによって録画が外から停止された際、即座にエンコーダや VirtualDisplay のリソースを解放する。これによりプロセスのゾンビ化とメモリリークを防止する。

### 5.3 パーソナライズの維持
- **座標の永続化**: フローティングボタンの移動完了後（`ACTION_UP`）、即座に座標を `SettingsManager` へ保存する。次回録画開始時にこの値を参照して復元することで、ユーザーが好む操作位置を常に維持する。

### 5.4 検知パイプラインの高度化
- **自己検知防止 (Masking Logic)**: OCRに画像を渡す前に、フローティングボタンの現在の画面上の座標を計算し、その範囲をBitmap上で「黒塗り」でマスクする。これにより、ボタン内のテキスト（SAVEやWO!）をWIPEOUTと誤検知するリスクを物理的に排除する。
- **リソース・リサイクル**: 検知ループ内で `Bitmap` を毎回新規生成（GC負荷）せず、同一のメモリ領域を使い回す（Recycling）。
- **非同期キャプチャ**: `PixelCopy` の実行には専用の `HandlerThread` を割り当て、メインスレッドのUI描画や録画ループの処理を一切妨げない設計とする。

### 5.5 シームレスな録画継続
- **フォーマット永続化 (Format Persistence)**: 30秒ごとのセグメント切り替え時、ビデオ/オーディオエンコーダの `MediaFormat` を保持しておき、新しいマクサー（MediaMuxer）へ即座にトラック追加を行う。これにより、切り替え時に発生しがちな「データの空白時間」を最小化し、シームレスなバッファリングを実現する。

### 5.6 A/Vタイムラインの物理的同期 (Timeline Alignment)
- **相対PTS計算**: ビデオとオーディオは独立したクロックで生成されるため、セッション開始時にそれぞれの「最初のデータが到着したシステム時刻」を基準点（Offset）として記録する。以降の全てのフレームは、この基準点からの経過時間としてPTSを計算・再割当てすることで、マージ後の動画における音ズレを物理的に排除する。
