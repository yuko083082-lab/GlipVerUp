# ZZZGlip 録画・WIPEOUT検知 仕様書

> **【開発鉄則】**
> **原因不明のまま「再起動」や「リトライ」による解決を図ることは厳禁。必ず真因を特定し、根本的な対策を講じること。**

---

## 1. システム構成（変更厳禁）

### 1.1 属性タグ (AttributionTag) の配置
- **マニフェスト**: `<attribution>` タグは必ず `<manifest>` 直下に配置すること。
- **Context**: システムサービスの取得には `createAttributionContext("glip_recorder")` を通した Context を使用すること。

### 1.2 VirtualDisplay の永続性
- **一回限りの生成**: 録画セッション中、`VirtualDisplay` を再作成してはならない。画面回転時は `resize()` と `setSurface()` で対応すること。再作成は Android 14 で `SecurityException` を引き起こす。

---

## 2. WIPEOUT検知ロジック

### 2.1 検知ループの堅牢性（防衛策）
- **リソース防護**: `PixelCopy.request` 実行前に `surface.isValid` を必須チェック。これはリトライではなく、無効なリソースへのアクセスによる**アプリ全体の強制終了を防ぐための「防衛」**である。
- **独立したライフサイクル**: 検知ループは録画の成否に依存せず、サービス開始と同時に起動すること。

### 2.2 ハイブリッド判定アルゴリズム
OCR 負荷の低減と検知率維持のため、以下の3条件を組み合わせる。
1. **色相判定 (HSV)**: 画面中央付近に黄色ピクセルが 0.008 以上の密度で存在すること。
2. **形状判定**: テキストブロックのアスペクト比が **2.5 〜 8.5** であること。
3. **論理和による確定**: 
   - 「ファジー一致（スペルがほぼ合っている）」
   - または「黄色密度が正常 かつ 特徴文字（W, P, U）が3文字以上含まれる」
   このいずれかを満たせば WIPEOUT と断定する。

### 2.3 判定の堅牢性（変更厳禁）
- **判定窓（Window Detection）**: 5フレーム中3フレーム以上の検知で確定。
- **検知ガード（Detection Guards）**: 検知が途絶えた場合、1.5秒以内に次が来なければ `matchCount` を 0 にリセットすること。これによりノイズの蓄積を防ぐ。
- **保存シーケンス (Timing)**: 
  - WIPEOUT検知から**5秒間**待機した後に保存を開始する。
  - 保存する動画の長さは**10秒間** (`10000L`) とする。
  - これにより、WIPEOUT表示の前後5秒間を正確に切り出す。
- **キーフレーム同期（Keyframe Alignment）**: セグメント（30秒ファイル）の切り替わりは、必ずビデオエンコーダからのキーフレーム（`BUFFER_FLAG_KEY_FRAME`）を待機して実行すること。これを怠ると、マージ後の動画が切り替わり目でフリーズする。
- **音声同期（A/V Sync）**: オーディオの Presentation Time Stamp (PTS) はシステム時刻ではなく、読み込んだサンプル数 (`totalAudioSamples * 1M / 44100`) から算出し、ビデオとのズレを最小限に抑えること。
- **Fragment Match（断片結合）**: OCRが文字を分割して読み取った場合でも、フレーム内の全テキストを統合して再判定すること。
- **Atomic Save**: 保存処理は `NonCancellable` コンテキストで実行し、いかなる場合も書き出しを完遂させる。

---

## 3. UI/UX・広告連携

### 3.1 視覚的フィードバック
- **「WO!」ボタン**: 保存処理中にボタンテキストを「WO!」に変更すること。ユーザーに保存が進行中であることを伝える。
- **透過制御**: 保存中はボタンの透過度を下げ、進捗インジケータを表示する。

### 3.2 広告表示 (Monetization)
- **Activity連携**: サービス単体では広告を表示できないため、保存完了時に `MainActivity` を `SHOW_AD` フラグ付きで起動すること。
- **Keyframe待機**: 広告表示要求を出す前に、必ずカレントセグメントの強制ローテーション（キーフレーム待機）を行い、最新の録画データが確実にファイルに書き出された状態にすること。
